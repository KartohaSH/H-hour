<h1>Orders Management</h1>

<% if @orders.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Order ID</th>
        <th>User</th>
        <th>Items</th>
        <th>Total Price</th>
        <th>Created At</th>
      </tr>
    </thead>
    <tbody>
      <% @orders.each do |order| %>
        <tr>
          <td><%= order.id %></td>
          <td><%= order.user&.email || "Guest" %></td>
          <td>
            <ul>
              <% order.cart_items.each do |item| %>
                <li><%= "#{item.product.name} (x#{item.quantity})" %></li>
              <% end %>
            </ul>
          </td>
          <td><%= number_to_currency(order.cart_items.sum { |item| item.product.price * item.quantity }) %></td>
          <td><%= order.created_at.strftime("%Y-%m-%d %H:%M:%S") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>No orders available.</p>
<% end %>
